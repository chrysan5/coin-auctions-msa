server:
  port: 10000

spring:
  application:
    name: common-module
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver  # MySQL 드라이버 클래스 이름
    url: jdbc:mysql://localhost:3306/common  # MySQL 데이터베이스 URL
    username: root  # 데이터베이스 사용자 이름
    password: 1234  # 데이터베이스 비밀번호

# actuator
management:
  endpoints:
    web:
      exposure:
        include: '*'
info:
  app:
    name: "Spring Boot Application - Common"
    version: "1.0.0"
    description: "Common ?? ???"